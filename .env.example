# Copy this file to `.env` in compose/pihole/ and set your own values.
# The real .env is git-ignored and must never be committed.

# Pi-hole web admin password
PIHOLE_PASSWORD=change-me-to-a-strong-password
